#ifndef _MAIN_H_
#define _MAIN_H_

/* Windows Defines */
#include <Windows.h>
#include <windowsx.h>

void InstatiateWindow(LPTSTR name, int nCmdShow);

static TCHAR NAME[] = "IP Analyser";
HINSTANCE hInstance;

#endif